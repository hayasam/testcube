# include basic requirements
-r ./requirements.txt

# for development
django-debug-toolbar

# for unit testing
coverage
django-coverage-plugin
faker
django>=3.2.23 # not directly required, pinned by Snyk to avoid a vulnerability
sqlparse>=0.4.4 # not directly required, pinned by Snyk to avoid a vulnerability
